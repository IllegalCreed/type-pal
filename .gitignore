node_modules/
dist/
build/
coverage/
*.log
.DS_Store
.idea/
.vscode/

# 原版游戏数据(版权归原始权利人,不入库)
data/raw/*
!data/raw/README.md
# OFL 字体 build asset,可入库
!data/raw/unifont-cn.bdf

# pal-extract 的产出(由原始数据生成,不入库)
data/extracted/

# TypeScript 增量构建产物
*.tsbuildinfo

# vite dev/build 需要 fetch data/extracted/,用 symlink 暴露(不入库)
packages/game/public/extracted
sdlpal.cfg

# 本地 brainstorm 参考资料 —— 含第三方版权内容(fan-made 攻略等),只本地用
reference/walkthrough/

# Playwright 工具临时缓存（快照 / 截图 / 控制台日志）
.playwright-mcp/
.playwright-cli/
output/playwright/

# 临时截图(本机 dev 验证用,版权 + 临时性质)
# 规范:dev verify 截图请保存到 build/dev-screenshots/(build/ 整目录已 gitignored)
# 下方规则保底兜根目录残留(M3 期遗留 m3-*.png,M3.5 t17-*.png 等)
m3-*.png
t*-*.png
*-dev-verify*.png

# Playwright L2 baseline PNG(版权,含原版游戏画面,本机生成不入库)
packages/game/e2e/baselines/

# Playwright 临时输出
packages/game/test-results/
packages/game/playwright-report/

# Claude Code 本地配置(每用户私有,不入库)
.claude/settings.local.json
.claude/

# 商业/私有文档(盈利策略 / 推广计划 / 商业模式 / 授权笔记等)
# 还原版仓库将开源(GPL),商业机密一律不入库,只本地留存
business/

data/baked/

# MG2 迁移事务 journal/staging/冲突报告(本地可恢复状态)
.type-pal-migrate/

# A7:可从受保护提取源/已选定 runtime 资产确定性重建，不提交原版/授权二进制副本
projects/pal/assets/migrated/
projects/pal/assets/runtime/

# MG2 首次 baseline 的一次性逐项审查报告；可由 bootstrap + 分类器重建
packages/migrate/bootstrap/pal.json

# N3-1 P2-P6 实验性 v5 影子 target；每批由权威 v4 纯重建，不作为 canonical 内容提交
packages/migrate/.shadow/

# IDE / Agent 自动生成的知识库、索引和本地计划（非项目真值文档，真值在 docs/ 和代码注释）
.qoder/
.zcode/
.mimosa/
