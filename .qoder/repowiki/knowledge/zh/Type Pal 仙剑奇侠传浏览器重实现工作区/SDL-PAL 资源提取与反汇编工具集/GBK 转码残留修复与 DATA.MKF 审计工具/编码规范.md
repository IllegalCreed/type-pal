- 对不可见或私有区码点使用 `String.fromCodePoint(...)` 数组字面量声明，避免源码中出现不可打印字符。
- 以常量表驱动映射逻辑：`FULL_LINE_FIXUP` 用 `Map<string,string>`、`PUA_CHAR_FIXUP` 用 `Record<number,string>` 集中维护 PUA→正字替换规则，核心函数只做查表与拼接。
- 先走快速路径（扫描 PUA 范围 U+E000–U+F8FF），命中后再进入慢路径，保证正常文本零开销返回。