- 对固定大小 chunk 在入口处用 `expectedSize` 常量 + `byteLength !== expectedSize` 校验并抛出带期望/实际值的错误消息。
- 使用 `DataView(buffer, offset, length)` 配合 `getUint32/getUint16(..., true)` 显式按小端序读取二进制,避免隐式字节序假设。
- C 结构体内存布局通过注释引用 sdlpal 源码行号(如 `map.c::PAL_LoadMap`、`global.c::PAL_LoadDefaultGame`)作为格式契约证据。
- 脚本入口 ip 值统一经 `labelOf(ip)` 转换:0→undefined,非零→`L_${ip}` 字符串,保持与 disasm 命名一致。