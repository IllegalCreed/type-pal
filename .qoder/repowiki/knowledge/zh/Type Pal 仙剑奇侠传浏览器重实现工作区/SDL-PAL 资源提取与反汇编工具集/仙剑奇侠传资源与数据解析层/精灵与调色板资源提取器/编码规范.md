- chunk 读取与 YJ2 解压由调用方完成,本模块仅接收已解压的 `Uint8Array` 并只做帧拼装与格式转换,保持单一职责。
- 缺失 chunk 时打印 `[pal-extract] ... skip` 警告后 continue,不抛异常,保证批量提取可容错。
- VGA 颜色升位统一使用 `(v << 2) | (v >> 4)` 整型运算替代浮点 ×255/63,兼顾精度与性能。