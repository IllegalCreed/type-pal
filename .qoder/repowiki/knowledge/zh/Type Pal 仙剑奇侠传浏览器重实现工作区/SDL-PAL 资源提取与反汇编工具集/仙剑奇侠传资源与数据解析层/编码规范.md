- parser 文件以 global.h 结构体或 MKF chunk 命名，导出纯函数接收 DataView/Buffer 返回 JSON 对象数组
- OBJECT 段起始索引、数量与字段偏移等跨 parser 常量集中在 `parsers/_utils.ts`，禁止在各 parser 内重复声明
- 测试文件与源码同名并列放置于 `__tests__/` 或 `.test.ts`，使用相同输入 fixture 验证解析结果