Node ESM (`.ts`/`.mjs` 配合 `import.meta.url`);CLI 用 `tsx` 直接执行脚本(`#!/usr/bin/env tsx`);视频转码依赖外部 `ffmpeg`(H.264 libx264 + AAC CRF 18);像素对拍使用 `pngjs` + `pixelmatch`;MKF/YJ2 解压复用本仓库 `src/io/mkf.js`、`src/io/yj2.js`。