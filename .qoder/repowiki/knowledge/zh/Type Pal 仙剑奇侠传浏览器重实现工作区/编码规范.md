- 所有可执行脚本通过根 `package.json` 的 `pnpm -r run` 或 `pnpm --filter <pkg>` 触发，禁止在子包内重复定义跨包编排命令
- 跨包复用能力（MKF/YJ2/RLE 解码、事件命令、帧率常量等）集中放在 `@type-pal/shared`，其他包仅消费其导出类型与函数
- 每个子包遵循 `check = typecheck && test` 的统一门禁约定，由根 `pnpm check` 一键聚合