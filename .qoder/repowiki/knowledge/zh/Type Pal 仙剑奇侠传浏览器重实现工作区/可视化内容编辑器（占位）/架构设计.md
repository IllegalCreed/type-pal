本包是 Phase 2 的占位骨架，src/index.ts 仅导出一个 EDITOR_PLACEHOLDER 常量，尚未包含任何业务逻辑。依赖边界通过 package.json 显式声明：仅依赖 workspace 内的 @type-pal/content（数据模型/schema），禁止触碰第一阶段包（shared / game / pal-extract）。构建与类型检查由 pnpm scripts 驱动（tsc --noEmit、vitest run），tsconfig.json 配合 monorepo workspace 使用。未来演进方向是在该包内集成 @type-pal/reforge 做实时预览，产出场景/对话/数据表等运行时数据文件。