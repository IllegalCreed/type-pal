第二阶段可视化内容编辑器。把 `projects/<id>/` 的工程 JSON 变成「可视化编辑 → 落盘 → 游戏生效」。独立 React + Vite app,中心是当前场景画面,侧边切「模式」,每个模式管一类编辑。渲染**复用** @type-pal/reforge(不重写);数据模型在 @type-pal/content。

状态(D-B0 地基就位):已从占位壳变成 React app —— 脚手架起得来(serveDir + loadProject 链通)、command/undo 纯核立稳。可视化编辑 UI(画布视口/布置模式/Inspector/File System Access 保存)是后续 B1+,尚未实现。详见 docs/phase2/editor/editor-design.md 与 editor-b0-plan.md。
