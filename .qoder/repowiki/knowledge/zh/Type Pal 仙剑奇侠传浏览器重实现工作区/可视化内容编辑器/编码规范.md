- `src/core/` 是纯逻辑(无 DOM、无 React)—— 可重度单测;React 只是它的视图,沿用本仓「纯逻辑与视图分离」惯例(如 reforge state 机)。Command 的 apply/invert 必须**不可变**(返回新 state,不原地 mutate;测里「源不变」钉这条)。
- 文件头 JSDoc 注释明确职责、依赖边界与阶段状态(editor 依赖 content + reforge,不碰 game/pal-extract),作为包的架构契约文档。
- 严守本仓 tsconfig:`noUncheckedIndexedAccess`(下标取值用前守卫或 `!`)、`verbatimModuleSyntax`(类型 import 用 `import type`)。
