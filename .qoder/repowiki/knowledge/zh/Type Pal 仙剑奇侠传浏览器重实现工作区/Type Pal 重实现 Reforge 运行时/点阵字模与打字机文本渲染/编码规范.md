- 渲染路径与可测试路径分离:纯函数(如 decodeGlyph、charsShown)只操作输入输出,DOM/Canvas 访问封装在 bakeGlyph/renderSpans 等浏览器专用导出中。
- 颜色统一以 `[r,g,b]` 三元组表示,通过 `colorRgba` 或固定常量获取,禁止硬编码 RGB 数值于调用处。
- 跨 span 的打字计数使用 `shown++` 累加并在 `maxChars` 边界提前 return,而非先求总长再切片。