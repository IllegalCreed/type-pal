- UI 全部基于 320×200 逻辑分辨率，绘制前 ctx.scale(WORLD_SCALE, WORLD_SCALE) 且 imageSmoothingEnabled=false，保证点阵像素锐利。
- 状态机采用「纯函数接收当前状态 + 返回新状态」模式（openXxx/moveCursor/confirm/back），主循环只负责根据按键选择分支并赋值。
- 异步资源加载一律 Promise.all 并发 + .catch 降级空集，确保单资源失败不阻断游戏启动。