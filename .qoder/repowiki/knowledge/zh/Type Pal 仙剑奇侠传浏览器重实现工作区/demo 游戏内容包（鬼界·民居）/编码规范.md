- 所有可被其他条目引用的实体（角色、物品、技能、对话、脚本等）均使用字符串 id 进行跨文件关联，而非路径或内联对象。
- 数值型效果（如 statBonus、healHp/healMp）采用统一的 effects 数组 + kind 字段分派模式，便于引擎扩展新效果类型。
- 资产路径在 manifest.assets 中集中声明根与各子目录，content 层仅通过相对路径引用，避免硬编码绝对路径。
- 本地化文案全部走 locale.json 的 key 引用（如 name.xxx、dlg.xxx），JSON 中不直接嵌入中文文本。