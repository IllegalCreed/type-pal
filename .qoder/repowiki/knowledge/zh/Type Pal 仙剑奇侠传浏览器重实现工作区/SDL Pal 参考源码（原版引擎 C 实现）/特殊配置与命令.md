更新参考源码需执行：
```sh
rm -rf reference/sdlpal
mkdir -p reference && cd reference
# 浅克隆并剥离 git 元数据
(git clone --depth 1 https://github.com/sdlpal/sdlpal.git sdlpal && rm -rf sdlpal/.git) || true
```
该目录不参与上层构建，仅供阅读对照。