- 所有对外导出函数均为纯函数，无 side effect，输入通过接口对象传入，RNG 通过 `SeedableRng` 注入而非闭包持有。
- C 语言 SHORT 语义统一用 `asShort(n)` 即 `(n << 16) >> 16` 模拟，并在每次赋值给中间变量后再次 asShort，确保与 sdlpal 每步 SHORT 截断一致。
- 对 sdlpal 源码行号使用注释标注来源（如 `fight.c:131-171`、`fight.c:4656-4658`），便于审计时 1:1 对照。
- 通过可选参数提供“旧路径回退”与“新 RNG 对齐路径”双分支（如 `party`/`enemySlots`、`preRolledAutoDefend`），省略时走兼容逻辑但分布相同、RNG 流不同。
- 魔法目标判定区分 `magic.type` 与 `flags.applyToAll`，强制全体集合 `ALL_TARGET_MAGIC_TYPES` 单独维护，避免二者不一致导致的行为漂移。