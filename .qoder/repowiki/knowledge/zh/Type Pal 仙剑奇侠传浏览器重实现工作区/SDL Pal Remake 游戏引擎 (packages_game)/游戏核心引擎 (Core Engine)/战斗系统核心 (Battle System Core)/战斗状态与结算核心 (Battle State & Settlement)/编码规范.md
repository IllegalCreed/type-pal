- C 结构体字段以 JSDoc 注释标注 sdlpal 源码行号与语义来源（如 `sdlpal fight.c:xxx`），TS 字段名与 C 成员一一对应，新增字段需补注释。
- 可选兼容字段统一用 optional 标记 + 运行时 `?? 默认值` 回退，保证旧 fixture 与单测不抛错。
- 战斗状态变更集中在 `createBattleState` 工厂与各 phase handler 内联 mutate，外部仅通过输入快照与 bus emit 间接影响，避免直接构造 `BattleState`。
- 面向 present 的视觉增量（动画帧、特效 overlay、伤害数字）采用声明式切片（`FighterDelta`/`BattleAnimOverlay`/`BattleAnimFrame`）而非命令式绘制，由 driver 在 tick 中 apply。