- 每个 perform 函数顶部用多行注释标注来源 `reference/sdlpal/fight.c:行号范围` 与 SDLPal 行为对照,作为真值契约。
- 对 C SHORT 截断统一封装为本地 `asShort(n)` 工具函数,避免散落的位运算。
- 动画路径优先:当 fighter 具备 `posOriginal` 时通过 `startBattleAnim` + `buildXxxTimeline` 挂帧;缺渲染状态则回退到同步 `bus.emit` 即时表现,保证旧 fixture 向后兼容。
- 脚本执行采用注入式 free function `runScript`(魔法与物品共享 `RunScriptFn` 类型),通过 `RunScriptOptions` 传入 `commands`、`runtimeMode='battle'` 与 `battleCtx`,便于单元测试 mock。
- RNG 调用严格遵循 C 左到右求值顺序,并在注释中逐条列出 `RandomLong`/`RandomFloat` 调用次序,确保确定性回放。