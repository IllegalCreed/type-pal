浏览器原生 API:`fetch` + `DecompressionStream('gzip')` 做零拷贝 gzip 解压(兼容 Chrome 80+/Safari 16.4+/Firefox 113+);PNG 走 `createImageBitmap` + Canvas2D `getImageData`;RLE/sprite-chunk 解析委托共享包 `@type-pal/shared` 的 `parseSpriteChunk`。