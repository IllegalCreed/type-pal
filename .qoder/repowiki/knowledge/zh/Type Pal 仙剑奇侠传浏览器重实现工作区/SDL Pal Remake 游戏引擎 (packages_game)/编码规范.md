- 每个业务文件旁放置同名 .test.ts 单元测试，e2e 用例按场景域分放在 e2e/{scene,battle,menu}/ 下并以 a/b/c 前缀排序。
- 所有运行时可见的视觉回归通过 e2e/baselines/*.png 像素基线比对，diff 图片用于审查变更。
- 核心逻辑与渲染严格分层：core 不依赖 present/shell，present 只读 core 状态，asset_pipeline 仅产出纯数据结构。
- 开发工具通过 vite 条件编译与运行时 flag 区分 DEV 构建与生产构建，避免污染产物体积。