--- a/main.c
+++ b/main.c
@@ -918,8 +918,11 @@
    // Show the trademark screen and splash screen
    //
    if (getenv("TP_DUMP_STATE") == NULL) {
-      PAL_TrademarkScreen();
-      PAL_SplashScreen();
+      const char *skip = getenv("PAL_SKIP_INTRO");
+      if (!skip || skip[0] == '\0' || skip[0] == '0') {
+         PAL_TrademarkScreen();
+         PAL_SplashScreen();
+      }
    }
 
    //
