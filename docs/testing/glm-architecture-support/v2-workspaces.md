# V2 · 工作区视觉/交互取证（ARCH-SUPPORT-GLM-1）

日期 2026-09-25（r2 返工同日）。冻结 SHA `3270473862…`。浏览器/环境同 V1（In-app Browser，实际看图；1440×1024 与
768×1024）。**环境披露**：画布内容渲染失败（tileset bytes 不符 / 标准色彩 JSON 404）为 worktree 资源
接入不完整（V0 已定性），本报告的布局判定全部**只依赖面板/工具栏/弹层等 DOM 结构**，不依赖画布内容。

> **r2 返工更正（Codex intake counter 0e751efe）**
> ① **对象列表折叠**：Codex 在 6010 实测往返正常（原生 AX 点击 checked 1→0、左分栏 194→0、再点恢复；
> Playwright DOM 核对该控件仍为 button，AX 呈 checkbox 不代表 DOM role 变更，且不把 GLM 的
> actionability 超时武断归因 role 错误；旧 (1064,18) 点击无命中见证）——**功能正常，不追不存在缺陷**；
> GLM 6013 环境未复现的原因未定位，操作链留档。② **r1 的 :568 归因更正**：MapMode.test.tsx:569-572
> 只调 verifyInspectorTabs（Inspector Tab 合同），不能证明分隔条键盘可达性。③ 剧情空态图不冒充
> ScriptEditor 编辑合同——空态仅证三栏结构；非空脚本工作区证据如实列为未完成（工作树基线项目共 0 条
> 可复用脚本，构造内容超出本包只读边界）。

## 证据链

| # | 操作链 | 实际（截图判读） | 结论 |
|---|---|---|---|
| V2-01 | 场景工作区初始态 | `v0 sample-01`（附 V0 报告）：左场景列表（294 项）+ 落点/实体分组 + 图层显隐复选组、右 Inspector（摘要/引用 tab）、三栏分隔条可见 | **covered** |
| V2-02 | 引用 tab 切换 | `v0 sample-02`：tab 切换高亮正确、阻断删除警示卡片结构完整、外部引用条目带打开按钮 | **covered** |
| V2-03 | 新建场景弹窗 + Esc 取消 | `v2-01-new-scene-dialog.png`：modal 覆盖层正确、名称字段预填聚焦（焦点环可见）、SceneId 校验提示文案完整、取消/创建动词按钮；Esc 后弹层关闭且场景计数保持 294（**取消零创建**，ARIA 快照复核） | **covered**（弹窗遮挡/焦点/Esc 保真三合一） |
| V2-04 | 地图模块（`?module=map&page=workspace`） | `v2-02-mapmode.png`：左地图列表（223 项，名称+ID+使用数徽标）+ 图层管理（眼睛/锁定/排序控件）、顶部工具栏（平移/选择/取样/笔刷/矩形/填充/擦除/碰撞）单行排列、右 Inspector（地图属性+选中图层）、绘制层级/显示高度控件在左下 | **covered**（布局层；画布空白系资源缺失，已排除） |
| V2-05 | 剧情模块（`?module=story&page=scripts`） | `v2-03-script-editor.png`：可复用脚本空态——左列搜索+空列表提示、中央"还没有可复用脚本/点击左侧加号创建"、右 Inspector"没有选中的共享脚本"；空态三栏结构完整无错位 | **covered**（空态布局；本基线项目共 0 条共享脚本） |
| V2-06 | 对象列表折叠 toggle | 两次 Playwright 定位点击超时（locator 纪律：不重试第三次）、改 CUA 坐标点击后画面无可见变化（`v2-04-outliner-collapsed.png` 与初始态一致） | **uncertain/blocked**——toggle 点击未产生可见折叠。**不判定为缺陷**：可能是点击坐标未命中或面板状态未变化；已按纪律如实记录操作与截图，留 Codex 复核 |
| V2-07 | 768×1024 全局降级 | `v1-05-party-768.png`（模拟器页）：菜单栏收纳为 文件/编辑/视图/导航；两列属性网格无截断 | **covered**（768 在规范支持范围；360 既有边界不重开） |

## 分类条目

- **V2-001 covered** V2-01~05、V2-07：三栏工作区、弹窗层级与焦点、tab 切换、空态、768 降级均有
  截图证据。
- **V2-002 covered（r2 归属更正）** 对象列表折叠**功能正常**——归属 Codex 6010 实测（往返
  checked 1→0→1、分栏宽 194→0→194）；GLM 6013 操作链（两次 locator 超时+CUA (1064,18) 无命中见证）
  如实留档，环境差异原因未定位、不武断归因。
- **V2-003 risk（r2 归因更正）** 分隔条拖拽/键盘操作无既有测试（r1 引 MapMode.test.tsx:568 有误——
  该用例只调 verifyInspectorTabs），本轮也未执行拖拽：如实列为**未验证项**，不为凑证据把相邻测试
  换名充数。
- **V2-004 covered（导航定位）** 切页后 URL 双写（`?module=X&page=Y` 可直达），跨页往返位置由
  App.reference-navigation 测试族覆盖（P1 对账表）；本轮 URL 直达场景/地图/剧情/模拟器四模块均成功。

## 768/1440 可执行复验矩阵

| 宽度 | 工作区 | 已证 | 复验截图 |
|---|---|---|---|
| 1440 | 场景/地图/剧情/模拟器 | 三栏布局+弹窗+空态 | v0 sample-01/02、v2-01/02/03 |
| 1440 | 对象列表折叠 | uncertain | v2-04 |
| 768 | 模拟器 | 两列网格+菜单收纳 | v1-05 |
| 768 | 场景/地图 | 未截（同链路可扩） | — |
| 360 | （既有披露边界） | 不测，不提新政策 | — |

## 未证风险

- 对象列表折叠行为（V2-002）与分隔条拖拽（V2-003）；
- 画布内视觉（tileset/调色板/精灵）全未判定——受 worktree 资源限制，且属正式资源链路，留给
  资源完整环境（6010/6051）复核。
