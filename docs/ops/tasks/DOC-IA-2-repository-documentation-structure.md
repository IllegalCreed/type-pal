# DOC-IA-2 - 全仓文档信息架构整理

Status: review
Owner: Codex
Phase: ops
Capability: ops（文档职责与引用，不改变能力状态）
Visual Verification Timing: N/A

## 用户授权与执行边界

2026-09-06 用户要求整理全部文档，明确允许 Codex 自行完成或制定计划交 GLM。
本轮由同一 Codex 独立执行，沿用已批准的文档维护工作，不重新要求三签；不修改产品行为、版本或机制。
DOC-GOV-1 已完成纠错与门禁，本卡处理它有意未做的物理归类和现行/历史拆分。

## 计划

1. 逐份登记文档去向，覆盖第一/二/三阶段、lore、ops、包与工程入口；原始第三方副本和就地来源说明保留。
2. 第二阶段顶层仅保留方针与进度入口；按 specs / guides / reference / archive 分类。
   现行内容、脚本、存档、编辑器架构、工作区合同从混合稿提取；旧稿完整保留为历史快照。
3. ops/tasks 保留活动任务与总索引；终态卡归 archive/tasks，模板、审计和证据分职存放。
   覆盖率与跨阶段 E2E 说明进入 docs/testing；第一阶段沿用清楚的编号概要/status/plans 结构。
4. 按显式 old→new 映射统一重定位本地链接、图片、目录入口、仓库路径及代码中的文档路径。
   Git commit:path 指的是历史树，不能随当前目录改写；签字、结论、公式与说明不得因链接处理而丢失。
5. 更新目录索引和文档检查器，加入路径重写、归档任务与布局回归检查；核对文件守恒、资产哈希和引用。
6. 实际跑文档门禁、相关工具/设计系统测试、必要的覆盖率检查与远端 CI；完成后提交推送并收口。

## 依据与完成条件

- 用户指出一阶段结构清楚、二阶段目录维度混杂；DOC-GOV-1 后已有链接与索引门禁。
- 根 AGENTS/CLAUDE 与阶段 READ-FIRST 仍是固定入口；只更新其中的文档位置，不改变规则。
- 每份原文档都有明确去向或索引重写记录；任何省略都必须有理由。
- 当前规范只维护一份；归档快照明确不承担当前执行授权，当前入口优先链接现行规范。
- 所有历史任务顶层状态与签字内容保持；历史素材与证明不删除。
- 本轮不重新裁决原版机制，不把历史缺口或计划变成新产品需求。

## 验证与回执

本地整理与验证完成，等待远端 CI 收口。

- 全量映射登记 419 个文件；最终保留并重定位 261 个文件，九张退役图片按用户意见退出工作树。
- 第二阶段顶层收敛为六份方针/入口，下面只有 specs / guides / reference / archive；混合合同已提取为
  五份独立现行规范，编辑器设计系统移入 specs。根文档、第一阶段、第三阶段、lore、ops、包/工程入口均已归类。
- 活动与终态任务分开；134 张原终态卡保持正文与签字，另 89 份历史记录保持正文。
  其余九张图片保持原始字节；机制文档只修一个章节跳转，公式和说明不变。
- 旧文档位置在链接、代码注释、测试读取路径与配置中统一更新；Git 历史对象引用保持原树身份。
  24 个源码/配置消费文件均验证为仅文档路径替换，无业务实现变化。
- 章节跳转已另行检查；场景入场指南的“每段都可设置”已按 author-script-core.ts 的 initial 节点校验
  修正，原说法保留在历史内容快照。
- `pnpm check:docs` 与工具反例测试 19/19 通过；读取设计规范的 boundary/adoption 测试 83/83 通过。
- 七包 typecheck 通过；`coverage:fast` 608 个生产文件 / 5,675 项测试通过，四项精确计数与原基线相同。
  未降低覆盖门槛，未改当前内容工程和迁移 baseline；既有 lint 债仍在后续 E-06 范围。
- [保真与清理回执](../archive/evidence/DOC-IA-2/preservation-report.json) 区分保存、迁移和退役附件；
  [原始映射](../archive/evidence/DOC-IA-2/relocation.json) 与补充映射只作本轮历史记录，不作为未来自动任务。

### 用户追加清理（2026-09-06）

用户指出 pal0/pal2-dialog 与两张 NPC 碰撞截图属于早期调试留图。已独立核实：当前代码、测试和规范
无引用，只有本轮新建索引引用；分别来自 `d42e781e` 与 `361eef8b` 的 2026-06 验收提交。
四张图退出当前工作树，空目录/本轮为其创建的索引一并移除；旧验收事实由 Git 保存，原图可恢复。
其余历史签字、公式、文字和有实际引用的证据继续保留。

用户随后指出 `docs/ops/samples/` 整组上传调试图同样无继续保留必要。已核实五张图仅被上述历史手工
验收清单引用，无当前代码、自动测试或工程资源依赖；整组退出工作树，历史清单注明 `b0930aa7` 恢复位置。
最终共清理九张已退役图片；其余九张图片均有具体任务卡/性能报告引用，保持原始字节。

## 交接

- 2026-09-06 Codex：用户授权全仓整理，先登记明确映射再执行；不依赖用户重复确认目录细节。

## 下一位 Agent 提示词

无下一位 Agent 提示词；Codex 完成整理、验证与收口。
