# 验收清单(做完划掉;不对就截图)

> 交付规矩:每项 = 直达入口 + 预置状态 + 逐键步骤 + 预期画面。
> 前置:三台 dev 在跑 —— 编辑器 http://localhost:6010 / 引擎(pal) http://localhost:6051 /
> 一阶段 https://localhost:6005(自签证书,点「高级→继续前往」)。
> F12 命令 = 打开浏览器控制台(F12 → Console)粘贴回车。

## ~~① W6 夜色观感~~ ✅ 2026-07-11 验收过(「特别好」)
- 夜:<http://localhost:6051/?scene=s042> → 连按 Enter 推完开场对话 → 隐龙窟外夜景
- 昼对照(同机位):<http://localhost:6051/?scene=s042&pos=79,43&facing=down>
- 预期:夜版整体压深蓝绿(红色门窗明显变暗、蓝色几乎不变)。
- 不满意 → 6010 编辑器 → 数据 → 🌗 氛围 → night 行点色块调色,存前试玩即时可看。

## ⑧ C3 装备页 UI 两连修(1 分钟,2026-07-11 修过重验)
- 入口:<http://localhost:6051/?scene=s002&pos=15,15> → F12:
  `__reforge.world.inventory.push({itemId:'168',count:1})`
- 步骤:Esc → ↓↓ 物品 → Enter → 装备 Enter → 列表选**铁剑** Enter → 换装面板。
- 预期:①左上卷轴框里物品图标贴**卷面上部深色区**(不再沉底);②「头戴/披挂…
  武术/灵力…」黑 label **笔画加粗**(原版 FBP 粗体观感;考证原版无阴影,
  单薄感来源是字形粗细,已按你拍板伪加粗)。商店 现有/金钱/售价 同步加粗。

## ~~② C3 装备手感~~ ✅ 2026-07-11 验收过(顺带揪出黑label无影+ITEMBOX图标偏下,已修 → 见⑧)
- 入口:<http://localhost:6051/?scene=s002&pos=15,15>
- F12:`__reforge.world.inventory.push({itemId:'168',count:1},{itemId:'175',count:1},{itemId:'237',count:1})`
  (铁剑 / 玄铁剑 / 布靴)
- 步骤:Esc 开菜单 → ↓↓ 到「物品」→ Enter → 「装备」Enter → 全屏列表选**铁剑** Enter →
  选李逍遥 Enter → 右侧五维变化(武术+10 防御+3)→ 再装**玄铁剑**(武术+70 身法-20 灵力-15
  防御+9;详情 4 行会自动上滚)→ 铁剑应回到背包列表。
- 预期:详情框效果行数字 = 属性实际变化(同一来源);换装旧件回包;布靴装脚部槽。

## ③ B5 战斗物品演出 + 战斗 UI 四修(2 分钟,2026-07-11 修过重验)
- 入口:<http://localhost:6051/?scene=s002&pos=15,15>
- F12:`__reforge.world.inventory.push({itemId:'61',count:3}); __reforge.startBattle(0)`
- 步骤:等李逍遥头上出三角(菜单态)→ ↓(杂项)Enter → ↓(道具)Enter → Enter(使用)→
  Enter(观音符)。再打一刀普攻(↑ Enter Enter)看敌怪掉血蓝字。
- 预期(全部照原版):
  - 三角贴**头顶正上方**(底中 −74 定锚,不随人物高矮漂);
  - 停一拍后,**连续滑步前移**(原版 6 步线性插值,非瞬移非变速)→ 到位举符,
    **举符 + "叮"音效 + 右上白字「观音符」三者同帧出现**,全身**闪白**(近乎全白,
    非微微变亮)呼吸一轮 → **先弹黄字回血数字 → 再瞬移归位**;
  - 杂项盒 61×112(实机对照一阶段量得;围攻/道具/防御/逃跑/状态五行全部装下),
    使用/投掷小盒 61×58;
  - 掉血/回血数字大致横跨怪/人中线(右对齐锚底中+6),每 40ms 升 1px、约半秒消失。

## ~~④ E9 商店/当铺~~ ✅ 2026-07-11 买卖都验过(顺带揪出对话期 unmigrated 0x8 警告,考证中)
- 商店:<http://localhost:6051/?scene=s050&pos=75,27&facing=up> → F12 `__reforge.world.money=5000`
  → Enter 和骆员外对话(话多,连按)→ 红框买菜单:↑↓ 选货,Enter → 确认框(默认「否」,
  → 或 ← 切「是」)Enter 买 → 左下「现有/金钱」变 → Esc 关店 → 掌柜收尾对话。
- 当铺:<http://localhost:6051/?scene=s029&pos=60,6&facing=up>(身上有刚买的货)→ Enter →
  全屏可卖列表(方向键 3 列导航)→ 选中看右下「售价」→ Enter → 「是」→ 钱+、货-。
- 预期:钱不够的货按 Enter 无反应;剧情道具(土灵珠等)不出现在卖列表。

## ~~⑤ E2E-1 空白工程 FSA 烟测~~ ✅ 2026-07-11 验收过
- 入口:<http://localhost:6010/?picker>(强制启动屏)
- 步骤:✨ 新建空白工程 → 选一个**空文件夹**授权 → 应直接进编辑器(地图模式,12×12 草地房)
  → 左上切「场景」模式 → 工具栏「＋添加实体」→ 右栏选个精灵 → 点画布放置 → 💾 保存 →
  📁 工程菜单 → 「📂 打开工程」→ 重选同一文件夹 → 刚放的实体还在。
- 预期:全程无红字报错;新建后立即可「🎮 引擎试玩」(出生草地房、四向能走、边界挡住)。

## ⑥ A4/A4c 素材上传全环(接⑤同一工程,10 分钟)
- 行走图:数据 → 精灵库 → 「＋ 上传精灵」→ 选 PNG(**4 行 = 下/左/上/右**,每行 N 帧)→
  确认切帧预览 → 入库 → 角色模式选 hero → 「精灵」下拉换成新传的 → 💾 保存 →
  场景模式 →「🎮 引擎试玩」→ 新页「▶ 开始试玩」授权 → **走两步看自己的小人**。
- 敌人外观:数据 → 敌人 → 选/新建一个 → 外观区「⬆ 上传外观」→ 选帧带 PNG(横排逐行切)→
  应用 → 预览台立即播待机动画 → 💾 保存 → 敌队「⚔ 试打」→ 战斗里看到自己的怪。
- 预期:未保存就能在编辑器里预览;保存后引擎里生效。

## ~~⑨ B5 战斗快捷键~~ ✅ 2026-07-11 验收过(「快捷键验收了没问题」)
- 入口:<http://localhost:6051/?scene=s002&pos=15,15> → F12
  `__reforge.world.inventory.push({itemId:'61',count:3}); __reforge.startBattle(0)`
- 菜单态逐个试:**D** 防御(立即过回合摆格挡姿)→ **E** 直开用品列表(Esc Esc 退)→
  **W** 直开投掷 → **R** 重复上回合动作 → **F** 全队强行普攻 → **A** 自动战斗
  (持续挂机,菜单态按 **Esc** 取消)→ **Q** 全队逃跑(战斗结束)。
- 预期:各键即按即生效,不用进杂项盒绕;A 挂机可 Esc 停。S(状态屏)暂缺 —— reforge
  还没有状态面板,面板落地后补。

## ⑩ M3 opcode 兼容层 + 扬州夜转昼(顺剧情验,无需专测)
- 商店买卖全程 console 无 `unmigrated op` 警告(已实测过,你玩时留意即可);
- 扬州客栈夜里剧情(s020)夜转昼应为 **3.2 秒渐变**(原版 PaletteFade 真值;
  此前 300ms 过快)。剧情推进到那段时看一眼。

## ⑪ B5 战斗 P0 效果接线:选队友 + 还魂香复活(1 分钟,2026-07-11 新)
- 入口:<http://localhost:6051/?scene=s002&pos=15,15> → F12:
  ```js
  const w=__reforge.world, li=w.party[0], ling=JSON.parse(JSON.stringify(li));
  ling.id='zhao-linger'; ling.template='zhao-linger'; ling.hp=0; ling.maxHP=200;
  w.party.push(ling); w.inventory.push({itemId:'95',count:1}); __reforge.startBattle(0)
  ```
- 步骤:菜单态按 **E**(直开用品)→ 选**还魂香** Enter → **进己方选人**(队员高亮闪,
  ←→ 切)→ 选倒地的灵儿 Enter → 举香演出 → 灵儿起身。
- 预期:灵儿以 **20/200**(10%)复活并在后续回合正常出手;还魂香扣 1;
  对活人用还魂香显「无任何效果」不白扣。夺魂/灵葫咒/偷窃/梦蛇 buff 等同批接线,
  顺剧情打到相应妖怪时留意即可(单测已钉:概率门/HP 门/灵抗门/收妖值/偷窃余量)。

## ⑫ B5 逃跑/变身/分裂演出(30 秒)
- 入口:<http://localhost:6051/?scene=s002&pos=15,15> → F12 `__reforge.startBattle(0)`
- 步骤:菜单态按 **Q** 逃跑。
- 预期:「嗖」音效 + 全队**连续滑向右下出屏**(约半秒多,不是瞬间消失)→ 回大世界;
  逃跑失败时(吉运低)= 向右下挪三小步 + 回身姿定格一拍 + 顶部「逃跑失败」。
  另三件顺剧情留意:敌整场逃离 = 全体向左滑出屏;变身怪现形 = 全身染白渐显一轮 +
  音效 + 换新形象(精灵中场重载,曾经是换了数据不换样子);分裂怪 = 分身从本体
  滑开落到各自站位(原版二分逼近手感,先快后慢)。

## ⑦ A5 导出 zip(1 分钟)
- 本地工程打开态 → 📁 工程菜单 → 「🗜 导出 zip…」(dev pal 工程该按钮是灰的,属正常)→
  下载 `<工程id>.zip` → 解压 → 与工程文件夹抽查比对(manifest.json / content/ / assets/)。
- 预期:结构一致、素材都在;有未保存改动时导出前会弹提醒。
