# 04 · 决策记录与待定问题

## 已拍板的决策

| # | 决策 | 说明 |
|---|---|---|
| D1 | 目标版本 = **1998 柔情版** | 非 1995 DOS 原版。数据格式按 Win95/98 系;有语音文件、AVI 过场。 |
| D2 | **原生 TS 重写**,不编译 C/WASM | 为了可读、可扩展。 |
| D3 | **忠实移植优先** | 第一目标是完整忠实跑原版;扩展能力是第二阶段。 |
| D4 | 不做通用游戏引擎 | 就针对仙剑这一个游戏;"可扩展"指这份代码以后好改。 |
| D5 | sdlpal 作**参考规格**,不 fork | C 源码当规格说明书,用 TS 重写。 |
| D6 | **两阶段架构**:离线 `pal-extract` + 运行时游戏 | 运行时完全脱离 MKF。 |
| D7 | 资源转现代格式;事件转**结构化 JSON** | 见 `02-architecture.md`。 |
| D8 | 移植方式 = **方案 B(分层重构移植)** | 忠实还原行为,但用模块化 TS 重新分层。 |
| D9 | 法律不在考虑范围 | 个人自用,不发布。 |

## 待定问题(下一个对话继续)

- **Q1 渲染技术**:Canvas 2D vs WebGL2。倾向先 Canvas 2D,够用再说。
- **Q2 表现层 / 外壳层细节**:主循环 tick 设计、输入映射、转场实现 —— 设计讨论到核心层就暂停了,这两层还没正式过。
- **Q3 events.json 精确 schema**:event-command 的具体指令集,对话 / 条件 / 序列怎么表达。
- **Q4 技术栈细节确认**:Vite + Vitest + pnpm monorepo 是否照此定。
- **Q5 98 柔情版数据文件清单**:需要拿到真实数据文件后,核对 MKF 文件名与内部格式(柔情版与 DOS 版有差异)。
- **Q6 符号表**:反汇编出的脚本里标志位是数字编号;是否边玩边补人类可读的名字。

## 设计讨论进度

brainstorming 已完成:可行性、目标、版本、移植方式、两阶段架构、资源 / 事件模型、核心层划分。

**未完成**:表现层 / 外壳层细节、events.json schema、技术栈最终确认。下一个对话从这里继续。

## 曾经考虑过、已否决的方向

- **方案 A(直译式移植)**:把 sdlpal 的 C 模块 1:1 翻成 TS。否决:会继承 C 的全局状态和大 switch,以后扩展痛苦。
- **方案 C(第一天就做现代脚本 API)**:否决:偏离"忠实移植优先",属第二阶段。
- **运行时直接解析 MKF**:否决:改为离线 `pal-extract` 一次性转换,运行时脱离 MKF。
- **把字节码当"资源"原样保存**:否决:字节码是程序不是资源,改为反汇编成结构化 JSON 事件 + 事件系统执行。
