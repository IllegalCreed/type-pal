# 能力地图（Capability Map）— 第二阶段的进度真值表

> **这是一份活文档。** 每做完一格、发现一格、改一格判据,都要更新它。它取代旧的 roadmap §8「复刻覆盖矩阵」当「第二阶段做到哪了」的真值。
> 配套阅读:[北极星与用法](roadmap.md) §8–§9 / [铁律](READ-FIRST.md) / [决策 D20](decisions.md)。
> 状态符号:`✅done` `⚠️半done(一边done一边缺)` `❌缺(两边都缺)` `—不适用`。
>
> **「能编」严格判据**(2026-07-05 立):编辑器侧标 ✅ 必须满足——能在编辑器里**真实创建/修改**该能力所需数据,**改动能保存**(dispatch Command → session → 工程文件)、**引擎能消费**。纯展示按钮、灰色 `soon` 占位、`defaultChecked` 假 toggle、空壳标签(点进去"待做")**都不算**——判据同 W1/W2「能开菜单 ≠ 系统能编」。「有 UI 入口但点了无功能」= ❌ 不是 ⚠️。

---

## 0. 一句话定位

第二阶段的目标不是「复刻原版」,是**一个现代化引擎 + 一个能让人创作新游戏的编辑器**。原版仙剑降级为「试炼场 + 第一份内容包 + 迁移器练手对象」——**不再是验收标准**(见 [roadmap §8](roadmap.md))。

这张地图把「第二阶段要做成什么」从一团迷雾变成 **8 领域、51 格** 的可数集合(活账,撞到新东西就长)。一格 done = 引擎能跑 AND 编辑器能编。原版撞到新东西就加格,地图会长大。

---

## 1. 北极星

> **一个现代化、先进、合理的引擎 + 一个编辑器,用户能用它创建新内容、新游戏包,甚至制作一款全新的游戏(素材剧情全替换)。**

判据层级(从高到低):
1. **架构层**:一个领域改内部实现,不牵动别的领域(`@type-pal/content` 契约切干净)。这是「现代化」的硬定义。
2. **能力层**:地图上的格,引擎能跑 + 编辑器能编。
3. **内容层**:能用这套平台跑原版内容(回归沙盒)+ 创作新内容。

---

## 2. 原版的三条用法

原版仙剑在第二阶段的角色**不是验收标准**,是三件事:

| 用法 | 判据 | 动作 |
|---|---|---|
| **A · 试炼场** | 一格 done = 能在原版找到**至少一个**真实例子跑通,**不要求**全覆盖 | 每格定义时点名一段原版当考题;考题过=done |
| **B · 第一份内容包** | 迁移器产物能在新引擎实际玩起来;覆盖度**不作里程碑门** | 维护回归沙盒 checklist(§6);能玩打✅,跑不了标缺啥 |
| **C · 迁移器练手对象** | 翻译成现代 schema,覆盖率可量,未覆盖有账 | 迁移器 pending 项是「翻译待办」,不是「验收欠款」 |

**红线(铁律 #6 不让步)**:原版隐式怪癖机制(冻帧副作用、palette 轮转、下标当身份…) **不进新引擎**。非要靠脏耦合才能跑的原版编排 → 标回归沙盒「需重写脚本」,不把脏东西搬进引擎。

---

## 3. 能力地图(8 领域 51 格)

> **格号 = 语义,不是顺序。** 格号按能力含义分配(W=世界/E=实体/C=角色/N=叙事/B=战斗/X=元层/MG=迁移器/A=资产分发),新格续号、表行可能不连续(如 B8 在 B7 前、C7 在 C6 前)——这是故意的,语义可读优先于行序整齐。

### 世界/场景(World)— 6 格

| 格 | 名字 | 引擎 | 编辑器 | 原版考题 | 备注 |
|---|---|---|---|---|---|
| W1 | 地图渲染(tile 墙面) | ✅ | ⚠️ | 客栈/盛渔村市集 | 引擎✅;编辑器只能摆实体,**不能画/改地图瓦片**(地图模式 `soon` 空壳、图层栏 checkbox 无 onChange 假按钮)。「实体摆放」归 E1;W1 编辑器侧 = 瓦片绘制工具,未实现 |
| W2 | 移动与碰撞 | ✅ | ✅ | 走路/撞墙 | done |
| W3 | 多场景切换 | ✅ | ✅ | 余杭→客栈传送 | done;switchScene/LRU/相机 |
| W4 | 入口/传送 | ✅ | ✅ | 门触发 | 编辑器(2026-07-05):场景检查器「命名入口」区(增删改名/坐标/朝向,空表收敛)+ loadScene 专表单(296 场景下拉/落点自定或进场点/朝向);画布拖拽摆点留后续打磨 |
| W5 | 音乐/氛围 | ✅ | ✅ | 场景 BGM | 引擎已播(场景 musicId/playMusic op/战斗曲+胜利小调/读档恢复);编辑器:场景 BGM 下拉+命令选择器+音乐库页(试听/起别名,music.json) |
| W6 | 时间/天气/昼夜 | ❌ | ❌ | — | 后处理合成;留口未实现 |
| W7 | 自有地图编辑器 | ❌ | ❌ | — | 新格(2026-07-05 作者点名):tile 编辑器(schema/tileset 导入/绘制工具/碰撞笔刷),摆脱 reuseOriginalMap 依赖。四期拆解与体量见 [editor-audit](editor/editor-audit-2026-07-05.md) §4;建议 E6 后立项,W7a schema 先行 |

### 实体(Entity)— 7 格

| 格 | 名字 | 引擎 | 编辑器 | 原版考题 | 备注 |
|---|---|---|---|---|---|
| E1 | 实体定义与摆放 | ✅ | ✅ | NPC/家具/宝箱 | EntityDef 二选一(actor/prop);编辑器(2026-07-05 两批):画布图层开关/菱形网格/禁入格 + 放置 palette(579 精灵过滤点选)/实体换精灵下拉/新建场景(复用地图起步)/换地图号即改即重载/⌘Z 撤销 |
| E2 | NPC 行为(走位/巡逻) | ✅ | ⚠️ | 市集游走 | autoScript;编辑器:事件模式可编 auto stages + 实体检查器「行为脚本」区一键跳转(2026-07-05);缺巡逻模板级编辑(来回走 A-B 等高级配置) |
| E3 | 可拾取物 | ✅ | ✅ | 宝箱/拾取 | done;拾取模板 |
| E4 | 触发交互 | ✅ | ✅ | 对话/事件 | done;事件模式 |
| E5 | 实体动画(loop) | ✅ | ✅ | 火把/流水 | 引擎(2026-07-05):loop 布局壁钟自循环 250ms/帧(loopFrameIndex,演出帧覆盖仍优先);编辑器:精灵库布局可编(C1d)。⚠ 两工程暂无 loop 精灵数据 —— 精灵库切 loop 布局即生效 |
| E6 | **实体定位权威** | ⚠️ | ❌ | 隐龙窟门口/试炼窟芦苇 | E6a 落地(2026-07-05,[设计](foundation/e6-position-authority-design.md)已评审):authority 表+主脚本/auto 双视图(位移指令隐式接管·仅被接管实体暂停 auto·脚本收尾统一归还);顺手修同实体 moveEntity 覆盖悬挂。已含 E6b 显式指令(takeEntity/releaseEntity,2026-07-05:schema+runner+双视图语义+表单+手册);待:E7 mount、N7 验收 |
| E7 | 载具/挂载 | ⚠️ | ✅ | 试炼窟芦苇漂(共乘)/坐船/骑驴 | 引擎(2026-07-05):mount 权威形态+每 tick 派生(位置=父+偏移,最高权威)+ mountParty/unmountParty/ride 指令(原版 0xA1+0x3F/44/97 的 clean 表达;demo 骑鬼演出垫实测:挂载/骑行相机跟随/连发/下载具全通)。编辑器:表单+手册已就绪。待:迁移器翻译原版骑乘链(芦苇漂考题跑通才 ✅) |

### 角色(Character)— 7 格

| 格 | 名字 | 引擎 | 编辑器 | 原版考题 | 备注 |
|---|---|---|---|---|---|
| C1 | 数据与属性 | ✅ | ✅ | 6 角色 | done;8 属性输入 |
| C2 | 精灵动画与姿势 | ✅ | ✅ | 走/特殊动作 | 引擎+姿势框选工具 done。⚠ 数据欠账(2026-07-05 作者指出):579 精灵 0 poses,特殊动作帧混标在行走布局里 —— 手工标注活,工具已备;精灵库缺人物/物件维度过滤 |
| C3 | 装备 | ⚠️ | ⚠️ | 6 槽装备 | 引擎逻辑已核(2026-07-05):equipItem 装上扣包/旧件回包/职业·持有校验(item.test 9 测)+菜单流 7 测+灵珠穿戴可用(itemmenu.c 对照);待作者游戏内观感验收后引擎列转 ✅。编辑器:equip JSON 可编,结构化编辑缺 |
| C4 | 头像/立绘 | ✅ | ✅ | 对话立绘 | portrait bake 88;编辑器(2026-07-05):角色模式立绘编辑(主号+预览图/表情增删改名/整组增删,UpdateActorCommand 可 undo) |
| C5 | 技能持有 | ✅ | ✅ | 仙术菜单 | 引擎 done(learnedSkills);编辑器(2026-07-05):SkillTab(90 技能列表/名字·说明·战外可用/消耗·目标·效果·动画 JSON 兜底,UpdateSkillCommand 可 undo) |
| C7 | 队伍管理(入队/离队) | ❌ | ❌ | 0x75 setParty×96 | 剧情队伍变动;多人战斗/菜单已就绪,缺世界侧指令。方案已拍板([D22](decisions.md)):reserve 暂存区,离队状态不丢;排 E6 后实施 |
| C6 | 成长/升级 | ✅ | ✅ | 升级 | 引擎:B7a 战后 exp/升级成长/学技能/半恢复(原版公式,实测);编辑器(2026-07-05):角色模式「升级」区(expTable 曲线 textarea 失焦提交·非法数字拦截 + 升级学技能行:等级/技能下拉/增删,UpdateLevelUpCommand 可 undo) |

### 叙事(Narrative)— 7 格

| 格 | 名字 | 引擎 | 编辑器 | 原版考题 | 备注 |
|---|---|---|---|---|---|
| N1 | 对话播放 | ✅ | ✅ | 客栈开场李大娘 | done;DialogueLine+立绘+翻页 |
| N2 | 事件触发 | ✅ | ✅ | onEnter/物品触发 | done |
| N3 | 脚本演出 | ✅ | ✅ | 客栈开场自动演出 | done;async 解释器+预览 |
| N4 | 事件模板库 | — | ✅ | — | done;编辑器产物,引擎无依赖 |
| N5 | 条件/变量/flag | ✅ | ✅ | 全局 flag | 引擎 done;编辑器(2026-07-05):数据模式「变量」页(flag/var 总览+读写明细)+ 物品页「被事件引用」区(737 处),点引用跳事件模式定位源(ref-index 全脚本递归扫描,含 branch/confirm/startBattle 子命令与 hostile.onLose) |
| N6 | 共享脚本/子程序 | ❌ | ❌ | — | DLC/批量期刚需,届时立项 |
| N7 | **演出接管** | ⚠️ | ⚠️ | 隐龙窟/试炼窟 | 显式 take/release;非冻帧;依赖 E6 |

### 战斗(Battle)— 9 格

| 格 | 名字 | 引擎 | 编辑器 | 原版考题 | 备注 |
|---|---|---|---|---|---|
| B1 | 回合战核心 | ✅ | ✅ | 开场李大娘战 | battle-core+BattleSession;敌人工作台(M4c-3:新建/数值/建队/试打) |
| B2 | 战场呈现 | ✅ | ✅ | 背景+站位 | 引擎 done;编辑器(2026-07-05):setBattleField 战场选择器(58 field 下拉,battle-fields.json 清单)+ 调色板着色预览(复用 loadBattleBg,0 号板;demo 无资产退化数字) |
| B3 | 指令菜单 | ✅ | — | 攻/术/防/物/逃 | 引擎 UI,编辑器无依赖 |
| B4 | 敌人 AI | ✅ | ✅ | 85 施法敌 | 策略/演出分层;AI 规则行编辑(M4c-3);难度预设留口 |
| B5 | 战斗动画/法术 | ⚠️ | ⚠️ | 施法演出 | M4d-2/2b:物攻双向/死亡淡出/施法+fire特效+音效(fight.c 真值)。⚠ 召唤全链缺(2026-07-05 作者问山神/雷神/武神查实):**迁移器没翻 type=summon 仙术**(山神/雷神/武神/剑神/酒神/风神/水神/火神/雪妖 9 技能缺失,火龙降级成普通伤害)+ 玩家侧 summon 效果执行器无 + 召唤神将演出无;变身/物品动画同待补。编辑器:技能编辑器已就绪(summon godId 字段) |
| B6 | 战斗触发与接入 | ✅ | — | startBattle | 事件侧触发;战斗侧无依赖 |
| B8 | 野外遇敌 | ✅ | ✅ | 野怪追人→碰撞开战→胜负回流 | s292 全链实测:追逐(chasePlayer 声明式)→touch 开战→败(渐红+胜败乃兵家常事也+读最近档)/胜(入账+怪消失 80s 重生);未翻译 7390→1947;概率停顿演出细节 lossy;编辑器经通用命令编辑覆盖 |
| B9 | 数据驱动敌对行为 | ✅ | ✅ | 林天南撑7回合/原地怪/重生窗 | EntityDef.hostile{team,chase,respawnSeconds,onLose}——零脚本,引擎 tickHostiles 驱动;编辑器:实体检查器「敌对行为」区(敌队下拉/追逐参数/重生秒/战败 gameOver·自定义命令 JSON) |
| B7 | 战斗结算 | ✅ | — | 经验/掉落 | B7a 入账/升级/学技能/半恢复 + B7b 忠实结算屏(经验金钱/升级8属性old→cur/练成) + B7c 隐藏经验(行为养成→属性提升弹窗)——原版 Phase A/B/D/E/F 全落 |

### 元层(Meta)— 7 格

> 元层格号用 **X0–X5**(2026-07-04 改名,原 M0–M5:与历史里程碑 M1–M4(§7)撞名,引用歧义)。

| 格 | 名字 | 引擎 | 编辑器 | 原版考题 | 备注 |
|---|---|---|---|---|---|
| X0 | 主菜单四项 | ✅ | — | 状态/装备/术/系统 | done;引擎 UI |
| X1 | **存档/读档+状态快照** | ✅ | — | 存档 | 完整流程实测(quick/manual 30 槽/位置+world 快照/同场景实体复位/跨刷新持久);auto 档挂演出链收尾钩(过门即触发);修场景缓存污染真 bug |
| X2 | 音频(BGM/SFX) | ✅ | ✅ | 场景音乐 | SFX+BGM 全落(SpessaSynth MIDI BgmPlayer 懒初始化/assetBase.music 资产链/系统菜单音乐·音效开关+localStorage 持久)。边界:X2=音频基建,W5=场景侧引用与切换 |
| X3 | 标题/流程/结局 | ❌ | — | 新游戏/通关 | 标题屏/流转全无 |
| X4 | 资源管线(RGBA 化) | ⚠️ | — | — | bake 双根落位:UI 皮→引擎 public(工程可覆盖 manifest.assets.ui)、内容资产(立绘/头像/物品图标)→ data/baked 库层;精灵/瓦片仍运行时 bake |
| X6 | 工程生命周期 | ✅ | ❌ | — | 编辑器侧:新建工程/FSA 打开本地工程/导出自包含包(玩家版闭环);引擎已能加载任意工程 |
| X5 | **跳转预览/沙盒启动** | ✅ | ✅ | 跳任意演出 | v1:编辑器事件页「🎮 引擎试玩」→ ?scene&pos&facing 落事件现场(邻格朝向,空格即触发);?pos = dev 语义跳过 onEnter(防剧情劫持落点)。世界态前置(flag 快照)待补——复杂剧情垫场景仍需真档 |

### 迁移器(Migrator)— 工具域,完成期退役 — 2 格

| 格 | 名字 | 引擎 | 编辑器 | 备注 |
|---|---|---|---|---|
| MG1 | 全量迁移 | — | — | ✅ done(M1-M4:数据/场景/脚本/敌人) |
| MG2 | 增量合并 | — | — | ❌ 已立项([D22](decisions.md)):迁移器会重跑,三方合并(base/theirs/ours)防覆盖手工改动。⚠ 落地前严禁重跑迁移器 |

> 迁移器格用 `引擎— 编辑器—`:工具域能力,引擎/编辑器不参与、不知道迁移器存在(阶段隔离铁律 D18)。完成期迁移器退役,MG2 随之废弃。

### 资产/分发(Assets & Distribution)— 在线编辑器架构([D21](decisions.md))— 6 格

> 架构前提([D21](decisions.md),2026-07-04):**编辑器 = 在线 Web 应用**(部署服务器,用户浏览器访问);**工程 = 用户本地**(FSA)。
> **工程自包含铁律**:工程目录是引擎加载时的全部世界,引擎打开工程 = 读这个目录的 manifest + content + assets,不会也不能去别处找素材。故**新建工程选「加载预制」时,预制素材必须从服务器下载并写进工程 `assets/`**——不缓存、不引用、不依赖声明,工程创建后即自包含快照。理由:① 浏览器读不到用户本地共享目录,「引用本地预制库」不可行;② 「依赖声明 + 服务器流式」离线即废,工程不自包含;③ 「预制库更新自动受益」是反模式——用户的工程被偷偷改素材是惊吓不是优点,要更新须用户**显式**触发(可选未来功能)。

| 格 | 名字 | 引擎 | 编辑器 | 备注 |
|---|---|---|---|---|
| A1 | 预制素材库(服务器端 + 版本化) | — | ❌ | 服务器托管 RGBA 素材包,带版本号;开发期=原版烘色,完成期=自有替代美术(避版权) |
| A2 | 新建工程:下载预制库写进工程 | — | ❌ | 选「加载预制」→ 从服务器下载 → 写进工程 `assets/` → manifest.assets 指工程内路径 |
| A3 | 新建工程向导(空白/加载预制 二选) | — | ❌ | 依赖 X6(✅引擎);空白 = 不下载任何东西,加载 = 触发 A2 |
| A4 | 用户上传自有素材 | — | ❌ | 导入 PNG/精灵表/瓦片集 → 写进工程 `assets/` + 注册 sprites/清单 |
| A5 | 工程自包含分发(打包导出) | — | ❌ | 工程始终自包含;导出 = 直接打包工程目录(zip),无「固化」步骤 |
| A6 | 预制库主动更新检查(可选) | — | ❌ | 用户显式触发:检查服务器新版 → 选择是否拉取覆盖。非自动;非 MVP |

> A 领域全部 `引擎—`:素材是数据,引擎只消费 manifest.assets 指针 + 工程目录文件,不关心源。所有动作都在编辑器侧(服务器通信 + FSA 写文件)。未决子问题(A1 版权策略与素材规模 / A2 下载协议与进度 / A4 支持的素材格式)留「做 A 领域」那轮 brainstorm 细化。

---

## 4. 阶梯依赖图(决定先做谁)

```
   ┌─────────────────── 高优先级地基链(剩余两条) ───────────────────┐

   ① E6 实体定位权威(地基,先做) ← 当前唯一剩余高优先地基链
        ↑
        ├─ E7 载具/挂载      (共乘/坐船/骑驴)
        └─ N7 演出接管       (显式 take/release)
              ↑ 叙事演出完整闭环

   └──────────────────────────────────────────────────────────────┘

   ✅ 已完成的高优先链:
   - B8 + B9 —— 战斗接入大世界全链 done(追逐/开战/胜负 + 零脚本数据声明)
   - X1 → X5 —— 跳转预览 done(验证痛点已解,编辑器「引擎试玩」跳到事件现场)

   顺带待闭合的半done格(2026-07-05 严格判据核后):
   W1 地图瓦片绘制(实体摆放✅/地图绘制❌) / E2 巡逻模板级编辑
   C3 装备观感验收+结构化编辑 / C5 技能数据编辑(空壳标签) / N7 依赖 E6
```

**剩余高优先级地基链**(2026-07-05 三次刷新,X5 已 done):
1. **E6 → E7/N7** —— 解队形vs演出vs载具打架(核心痛点)。E6 是地基,**当前唯一最高优先**。

> 下一轮选择器推荐:W1 编辑器侧(地图瓦片绘制)是阶梯上较大的缺口——一个不能画地图的 RPG 编辑器谈不上"能造新游戏"。E6 完成前若想换换节奏,W1 是合理穿插。

---

## 5. 下一步选择器(每次开工先跑这个)

```
1. 扫地图,找所有 ⚠️ 半done格 → 有?
     → 挑 ROI 最高的(另一边已done,补缺口即闭合) → 做
   无 ⚠️ ↓
2. 找所有 ❌ 但下游有依赖的格 → 挑解锁最多的 → 做
   无明显高价值 ↓
3. 挑阶梯最底部的 ❌ 格 → 做
   都没有 ↓
4. 当前可见全 done → 跑回归沙盒(§6) → 撞到新东西 → 开新格
```

### 三规则

1. **先闭合半done(⚠️),而非开新格** —— 半done格回报最高(已有投入兑现成完整能力)。
2. **开新格前问「它解锁了谁」** —— 选下游有依赖者优先(X1 解锁 X5、E6 解锁 E7/N7)。
3. **同等优先级,先做阶梯底部** —— 地基先于上层,防返工。

### 纪律:一轮一承诺

选择器这一轮给出的格,**钉死做完(或明确判定不做)才进下一轮**。不允许「做一半又怀疑该不该做」——否则又陷入「是不是该做 E6 而不是 B1」的无限纠结。选择器是承诺机制,不只是建议。

---

## 6. 回归沙盒(原版跑得通吗)

> 取代旧 roadmap §8 复刻矩阵。**它是体检表,不是验收表。** 能跑打✅,跑不了标缺什么能力格——能力格做了再回来看。打满不是目标。

(初始版本,跑原版时滚动更新)

| 原版内容 | 状态 | 缺口 |
|---|---|---|
| 客栈开场演出(李大娘对话) | ✅ | — |
| 盛渔村市集 NPC | ✅ | — |
| 门传送(场景切换) | ✅ | — |
| 开场李大娘战 | ✅ | B1✅(敌人工作台)/B5✅(动画)/B7✅(结算);全链 done |
| 隐龙窟门口演出 | ❌ | E6 定位权威/N7 演出接管 |
| 试炼窟芦苇漂(共乘) | ❌ | E6/E7 载具挂载 |
| 升级流程 | ✅ | B7a/B7b/B7c 全落(入账/成长/学技能/半恢复/结算屏/隐藏经验) |
| 场景 BGM | ✅ | X2 播放器(audio/bgm.ts)+ W5 接线(场景槽/playMusic/战斗进出/读档),2026-07-05 |
| 机制道具(紫金葫芦收妖/炼蛊皿/遇敌香…37 件 pendingUse) | ❌ | 物品机制格(未开):引擎做**通用机制**(收集计数器/定时炼化/遇敌率修正),道具数据引用机制+参数——不硬编码具体道具(2026-07-04 口径,同敌对行为数据化) |
| 召唤仙术(山神/雷神/武神/剑神/酒神/五灵神/雪妖/火龙) | ❌ | 召唤全链:迁移器补翻 type=summon → 引擎效果执行 → 召唤演出(B5);编辑器已就绪 |
| 标题屏/新游戏 | ❌ | X3 流程 |
| ... | ... | 跑到哪填到哪 |

---

## 7. 和 M1–M4 的关系(不推翻)

M1–M4 不作废。它们是**已done的能力格的历史名字**:

- **M1 数据表** = 数据可定义格(C1 数据 + 数据模式)
- **M2 全场景静态** = W1/W2/W3 + E1 场景格
- **M3 脚本翻译** = N1/N2/N3/N4 叙事格
- **M4 战斗** = B1–B9 战斗格(引擎侧基本全 done,B5 变身/召唤、B2/B9 编辑器侧待补——见 §3 战斗领域表)

以后不再用里程碑编号,直接用格 id(「做 E6」而非「M5 做实体定位权威」)。

---

## 8. 能力格发现协议(地图怎么长大)

撞到「原版有、新引擎表达不了」时,标准动作:

1. **先判是不是铁律 #6 该绕开的怪癖**(冻帧副作用、palette 轮转…)。
   - 是 → 标回归沙盒「需重写脚本」,**不开新格**。
2. **不是怪癖,是新能力** → 开新格:
   - 标在哪个领域(8 领域之一,或新领域)
   - 引擎侧/编辑器侧现状
   - 原版考题
   - 阶梯位置(依赖谁、解锁谁)

> 原版一个「机制」在新地图里常裂成多格——原版用脏耦合把多个独立功能焊一体,新引擎按职责拆开(冻帧→E6/N7/E7 三格;box-style→对话/旁白/物品框各自归系统)。地图因此长格,但长的格**少于**原版脏机制数(很多是同类反模式)。

---

## 9. 这张地图怎么用(每次开工标准动作)

```
1. 读本文 §5 选择器 → 决定下一步格
2. 该格的原版考题 → 进 docs/phase2/<领域>/ 找/写它的 spec
3. 实施(引擎侧 + 编辑器侧两边都 done 才算)
4. 更新地图(该格标✅)+ 回归沙盒(考题跑通打勾)
5. 下一轮回 1
```

**这就是从「脱离控制」到「重新掌舵」的转变**:下一步不再靠直觉或 Claude 提议,是读图。
