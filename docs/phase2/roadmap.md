# 第二阶段路线图 — 重制引擎 + 内容编辑器

> 状态：活文档（2026-06-17 起草，2026-07-13 补全执行路线）。本文是第二阶段的**总纲 / 北极星**，只定方向、依赖顺序与完成口径；每项实现细节另见能力地图、任务卡和各自 spec。
> 文档边界：`docs/` 顶层与 `docs/plans/` 服务**第一阶段（忠实还原）**，本目录（`docs/phase2/`）服务**第二阶段**。两者不混。

## 0. 三阶段愿景

| 阶段 | 目标 | 状态 |
|---|---|---|
| 一 | 100% 忠实还原原版仙剑（字节级对齐 pal.exe） | 接近尾声 |
| 二 | 在还原成果上建立可**修改 / 创作**自有内容的现代引擎与编辑器，并保证工程资源自包含、可替换 | ← 本文 |
| 三 | **版权资源替换 + 官网 + 离线发行 + 用户系统**：批量替换原版受保护表达，建设项目官网、离线 EXE 发行版（Tauri 封装引擎与编辑器）及用户系统（认证 + 云存档 + 在线编辑器） | 第二阶段收口后启动 |
| 远期 | 基于第二阶段成果制作 MMO | 远期设想 |

## 1. 第二阶段是什么

把项目从「只能播放原版内容的忠实复刻」，升级为「能用编辑器**自己生产内容**的现代化引擎」。两件大事：

1. **新引擎**：现代化、场景解耦的运行时，替换掉为忠实还原而生的全局耦合数据模型。
2. **内容编辑器**：可视化编辑地图、场景事件、数据表，产出独立于原版的内容工程。

完成后，创作新内容不再依赖从原版 MKF 提取，并具备导入、替换和自包含分发作者资源的完整能力；
原版受保护资源的实际批量替换与版权发布验收归第三阶段。

## 2. 硬约束（已与作者对齐）

- 内容源**独立、版本化**；初始化 = 从 `data/extracted/` 复制一份，此后独立演进。
- 事件编辑**必须可视化**（不要求作者读字节码）。
- 现有 `@type-pal/game` 引擎**冻结不动**，新引擎另起 package。
- 新引擎**现代化**：场景自包含（map + sprites + events 随场景按需加载），跨场景影响走全局层；消除 `all.json` 全局脚本、per-role HP 这类全局耦合。
- 为第三阶段 MMO **预留状态分层**（玩家私有 vs 世界共享），不挖死胡同。
- **开发期 current-only**：正式上线前只支持当前 canonical 内容/存档格式；一次重迁完成后，同任务删除旧
  upgrader、旧类型、fixture、版本分支、产品升级入口与兼容 fallback，历史由 Git 保存。

## 3. 关键架构判断

1. **旧 game / 原版 = 内容参考，不是实现真值锚。** 第二阶段是全新创作：新引擎**不要求行为与旧 game / 原版逐状态一致**，没有「双引擎对照」（那是第一阶段打法）。旧 game 与原版只用来回答「这场戏 / 这个系统原本是什么样」，作内容与玩法参照；实现怎么做、数据怎么放，由新架构自己定。
2. **架构全新重写，移植已验证的逻辑知识。** 战斗公式、164 个 opcode 语义、present 渲染数学是几十个 commit 啃出来的**知识** → 可移植 / 重新实现，但**不照搬旧的模块结构**（那会把旧架构的耦合一起带进来）。新引擎从架构出发重新组织。
3. **真正的地基是内容 schema。** 引擎与编辑器都长在其上。「场景层 vs 全局层」的边界是第二阶段最核心的设计。
4. **引擎先吃通 schema，再做编辑器。** 否则 schema 未经运行验证，编辑器返工。
5. **schema 给 MMO 留口。** 状态分层（玩家私有 / 世界共享）在单机阶段就埋好。
6. **杜绝「下标 / 数组位置当身份」。** 对象、引用、状态键一律用稳定 id / 语义名 —— 加删内容不牵动他者、跨场景引用可读可编辑。解耦与编辑器友好的硬性原则。

## 4. 顶层分解

| 子项目 | 内容 | 依赖 |
|---|---|---|
| **P0 · 内容 Schema + 迁移器** | 定义场景自包含的内容格式 + 全局层边界；写 `extracted → 内容工程` 的一次性迁移 | — |
| **P1 · 新引擎 package** | 架构全新重写，移植已验证逻辑（战斗 / opcode / 渲染），按 P0 schema 组织、场景懒加载；核心系统（对话 / 战斗 / 存档 / 事件）逐步重做；验收 = 正确跑迁移内容 + 架构现代化 / 解耦 / 可扩展 | P0 |
| **P2 · 编辑器 package** | 读写内容工程：地图笔刷 + 可视化事件编辑 + 数据表编辑；嵌 P1 做实时预览 | P1 |
| P3 · 素材导入与替换工作流 | 导入 / 创作作者资源（瓦片 / 精灵 / 音乐 / 字体），并以稳定引用替换工程资源；版权素材实际批量替换归第三阶段 | P2 |
| P4 · MMO（远期，非第二阶段） | 基于第二阶段成果的远期设想 | 第二阶段收口后另议 |

第二阶段依赖链为 **P0 → P1 → P2 → P3**；P4 已移出本阶段，不参与第二阶段完成判定。

## 5. 切入策略：骨架先行 + 垂直切片

不在白板上把 schema 一次设计完美（瀑布陷阱）。P0 先定 schema **骨架**（重点是场景层 / 全局层边界），立刻用 P1 一个**垂直切片**验证：让新引擎跑通一个最简单的场景，边跑边补。

**首个垂直切片目标（暂定，作者可调）**：一个室内小场景（客栈 / 民居类）—— 加载地图 + 几个 NPC + 一段对话，在新引擎跑通；编辑器能改对话、加一个 NPC。作为第一个看得见摸得着的 milestone。

## 6. 文档与工作流约定

- 本目录（`docs/phase2/`）= 第二阶段全部文档；`docs/` 顶层 + `docs/plans/` 冻结为第一阶段资产。
- **第一阶段文档对第二阶段的定位（重要规矩）**：`docs/` 顶层 + `docs/plans/` 是第一阶段真值，做第二阶段时它们**只作「原版 / 旧引擎行为参考」**（性质同 `reference/sdlpal/`），**不可当作第二阶段的现状**。第二阶段的「现状」只看 `docs/phase2/` + 新代码。
- 每个子项目出独立 spec（`docs/phase2/p0-*.md` 等）；决策记入 `docs/phase2/decisions.md`（待建）；现状真值表待各子项目落地后再建。
- 索引见 `docs/phase2/README.md`。

## 7. 决议与待定

**已决（2026-06-17）**

- ✅ **首个内容目标**：室内小场景（客栈 / 民居）跑通 + 改对话 + 加 NPC（见 §5）。
- ✅ **代号：Reforge（重铸）** —— 把忠实还原「重铸」为可创作的现代引擎。
- ✅ **受众**：先按**单人本地工具**设计，不做账号 / 协作 / 云同步 / 分发；但内容工程走「文件 + 版本化（git 友好）」，架构**不堵死**将来协作（与 §3 第 5 条的 MMO 留口一脉相承）。
- ✅ **package 架构（[D18](decisions.md) 厘清职责 + 阶段隔离）**：`@type-pal/content`（**内容数据模型** = schema + 纯逻辑）、`@type-pal/reforge`（新引擎）、`@type-pal/editor`（编辑器）、`@type-pal/migrate`（**迁移器**，独立成包、不再塞进 content）。内容实例（场景 / 对话数据）= 内容工程数据目录（非 npm 包）。**阶段隔离**：第二阶段代码不碰第一阶段包（shared / game / pal-extract），迁移器是唯一桥。

**已决（2026-06-22）**

- ✅ **P0 schema 草案**：见 [content-schema.md](foundation/content-schema.md)（三层状态 / 稳定 id / 场景包 / 事件演出 / 角色实例化 + 迁移器）。
- ✅ **第一阶段引擎架构债已审计**：见 [engine-debt-audit.md](foundation/engine-debt-audit.md)（18 条 finding，P0/P1/P2 分级 + 反查表），是 P1 新引擎 spec 的**反面输入**——重写时绕开这些模式，不照搬旧模块结构（铁律第 3 条）。

**进展（2026-06-28 更新）**

- **P1 切片 1 已起**：室内场景跑通（地图 / 移动 / 碰撞含 NPC / 遮挡 / 对话）；对话现代化（结构化 + i18n + 外观 + 去 palette，见 [D11](decisions.md)–[D15](decisions.md)）完成。
- **前置 [D16](decisions.md) 渲染地基已落地**（2026-06-28）：逻辑 / 显示分离（菱形轴格坐标 `GridPos={col,row,height}` + 物理 1280 + UI 高清化，[render-foundation-plan](foundation/render-foundation-plan.md)）。它是菜单及后续所有 UI / 美术 / 编辑器的底座。**下一步 = [D17](decisions.md) 菜单**（设计已定、地基就绪可落地）。
- **之后**：菜单 → 多场景 / 事件演出 → 编辑器（P2）。schema 边跑边补（§5 切入策略）；P1 spec 输入 = [content-schema](foundation/content-schema.md) + [engine-debt-audit](foundation/engine-debt-audit.md)。

**进展（2026-07-02 更新）**

- **A 期工程化落地**：壳/肉分离（`projects/<id>/` 工程 = manifest + content JSON + assets 自包含），reforge 零具体游戏 import（[project-design](editor/project-design.md)）。
- **编辑器 B1 布置模式 MVP 落地**：模式壳 + 画布复用 reforge 渲染 + command/undo + 选/拖/增删/FSA 保存（[editor-design](editor/editor-design.md)）。
- **角色/精灵动画模型与创作闭环已落**：统一 ActorDef + SpriteLayout（[actor-model-design](foundation/actor-model-design.md)）；C1-1 已于 2026-08-14 完成角色 CRUD、预制人物/自定义实体双轨、引用处置和用户验收；C1-2 完成 content14 结构化对话人物身份；C1-3 第一批经用户 exact digest 审批后发布李大娘/酒剑仙 2 个 NPC Actor、6 个 entity 与 163 个 cue，三方审查和用户验收均完成。未批准候选继续保持 deferred。
- **编辑器后续整体审查方向已记录（2026-08-14，2026-08-15 用户补充拍板）**：B2 完成后先冻结一份
  完整、可执行、可验收的编辑器设计规范，再以该规范为尺分三线审查全编辑器——视觉/交互统一、ED-1
  七环创作闭环、Reforge/Editor 代码质量与重构。角色模块与战场模块是参考输入，不是两套各自扩张的
  局部标准；资源图像预览（当前挤在上方且不能缩放）和战斗模块布局是首批反例。规范未冻结前不得逐模块
  凭感觉翻新；B2 与 ED-DS-1 已于 2026-08-15 用户验收 done，设计规范 v1.0.0 已冻结。下一步为
  ED-DS-2 代码化 tokens、shared primitives 与 Design Lab。见
  [editor-modernization-follow-up](editor/editor-modernization-follow-up-2026-08-14.md)。
- **脚本兼容决策拍死（原 content-schema §6 待定项）**：迁移器把原版脚本**翻译**成结构化脚本（[script-system-design](foundation/script-system-design.md) 的 AST），**不建永久 opcode 兼容执行器** —— 双解释器 = [engine-debt-audit](foundation/engine-debt-audit.md) P0-5/P0-6 的债重生。翻不净的意大利面脚本（有限集）迁移器标注 + 编辑器手修。

**进展（2026-07-04 更新）**

- **事件模式编辑闭环落成**：演出预览（播放 / 单步 / 命令高亮 / 日志桩）+ 脚本编辑 v1（选行改参 / 插删移 / undo / 保存）+ 事件模板库（按 4382 段触发脚本形状统计提炼：宝箱 / 拾取 / 得钱 / 跨房间镜头 / 钻洞 / 搭话，插入即展开、「自身」感知）。
- **事件抽象三层定调（2026-07-04 用户）**：① schema 命令 = opcode 语义抽象（已有）；② 编辑器插入模板 = 成组模式展开（已落地，不做黑盒高层命令）；③ **共享脚本 / 子程序**（callScript 的 clean 版：同一逻辑多处引用、改一处全生效）——**DLC / 批量内容期的刚需，届时立项**；迁移器现按内联展开，升级时需去重提炼。
- **N6b 意图式创作裁决（2026-09-03 用户拍板）**：第二阶段在薄 E2E 基线后完成四种内置取物意图、
  类型化参数持久化、业务表单回编辑和单向展开；2026-09-05 因 SceneIndex 先占用 content20，N6b 原子
  切换顺延为 content21。作者自定义模式、表达式、组合及
  完整对话/演出工作台留到第三阶段。现有 N6 v1 仍保持完成，N6b 不反向降级它。范围与顺序见
  [design-backlog N6b](design-backlog.md#n6b-强类型脚本模式库)。

## 8. 北极星与原版定位（2026-07-04 重组，废原 §8「100% 复刻 = 验收」）

> **本节取代旧 §8「硬验收：完整复刻原版」。** 旧版把「原版全流程通关 = 验收」钉成 done 标准，与铁律 #6（现代化优先，不复刻怪癖）直接冲突，导致每步推进都在「要不要为复刻忍脏」上反复拉扯。

### 北极星（用户 2026-07-04 原话定调）

> **一个现代化、先进、合理的引擎 + 一个编辑器，用户能用它创建新内容、新游戏包，甚至制作一款全新的游戏（素材剧情全替换）。**

判据层级（从高到低）：
1. **架构层** —— 一个领域改内部实现，不牵动别的领域（`@type-pal/content` 契约切干净）。这是「现代化」的硬定义。
2. **能力层** —— [能力地图](capability-map.md) 上的格，引擎能跑 AND 编辑器能编。
3. **内容层** —— 能用这套平台跑原版内容（回归沙盒）+ 创作新内容。

### 原版仙剑的三条用法（不再是验收标准）

| 用法 | 判据 | 动作 |
|---|---|---|
| **A · 试炼场** | 一格 done = 能在原版找到**至少一个**真实例子跑通，**不要求**全覆盖 | 每格点名一段原版当考题；考题过 = done |
| **B · 第一份内容包** | 迁移器产物能在新引擎实际玩起来；覆盖度**不作里程碑门** | 维护回归沙盒（[capability-map §6](capability-map.md)）；能玩打✅，跑不了标缺啥 |
| **C · 迁移器练手对象** | 翻译成现代 schema，覆盖率可量，未覆盖有账 | 迁移器 pending 项是「翻译待办」，不是「验收欠款」 |

**红线（铁律 #6 不让步）**：原版隐式怪癖机制（冻帧副作用、palette 轮转、下标当身份…）**不进新引擎**。非要靠脏耦合才能跑的原版编排 → 标回归沙盒「需重写脚本」，不把脏东西搬进引擎。

### M1–M4 不作废

它们是**已 done 的能力格的历史名字**（M1=数据格 / M2=场景格 / M3=叙事格 / M4=战斗格进行中）。以后不再用里程碑编号，直接用格 id。详见 [capability-map §7](capability-map.md)。

## 9. 能力地图 + 下一步选择器（2026-07-04 新增）

第二阶段「做成什么样 / 做到哪了 / 下一步做啥」的真值，全部收敛到 **[能力地图 capability-map.md](capability-map.md)** —— 它取代旧 §8 复刻矩阵当进度真值表。

**核心机制（一句话）**：8 领域、63 格；一格 done = 引擎能跑 AND 编辑器能编；下一步不靠直觉，跑[选择器](capability-map.md#5-下一步选择器每次开工先跑这个)读图。

每次开工标准动作：
```
1. 读 capability-map §5 选择器 → 决定下一步格
2. 该格的原版考题 → 进 docs/phase2/<领域>/ 找/写 spec
3. 实施（引擎侧 + 编辑器侧两边都 done 才算）
4. 更新地图（该格标✅）+ 回归沙盒（考题跑通打勾）
5. 下一轮回 1
```

能力地图是**活文档**，撞到原版新东西就加格（[发现协议](capability-map.md#8-能力格发现协议地图怎么长大)）。原版一个脏机制在新地图里常裂成多格（冻帧 → E6/N7/E7 三格）。

## 10. 从当前工程到完整产品的执行路线（2026-07-13）

### 10.1 四份真值各管什么

| 文档 | 只回答什么 |
|---|---|
| 本路线图 | 最终还缺哪些大块、先后依赖和每一波的退出条件 |
| [能力地图](capability-map.md) | 引擎/编辑器每个能力格当前是否真的完成 |
| [三贤人看板](../ops/board.md) | 眼下正在做什么、下一位是谁 |
| [任务卡](../ops/tasks/README.md) | 单项设计、签字、实现证据、验证与交接 |

聊天记录不承担路线图职责。以后用户追加长期目标，先写回本节或能力地图，再开任务卡。

### 10.2 最终完成不是一个 `done`

第二阶段必须同时达到四个终点，缺一项都不能称为本阶段完成：

1. **创作平台完成**：新引擎能运行、编辑器能创作完整 RPG 内容，稳定 schema 上的常用工作流无需手改 JSON。
2. **首个内容包完整可玩**：迁移后的 `projects/pal` 能从开场一直走到结局；不是只验证若干能力考题。
3. **工程资源自包含与预制发布源**：工程拥有的运行内容与素材全部在工程目录内，运行时不再偷偷读取仓库级 `/extracted`、`/baked` 或其他外部素材目录；默认 UI、字形、光标和标题由引擎壳随 bundle 提供，不复制进工程。A1 服务器版本化预制工程资源库在第二阶段最终发布前收口。
4. **可独立打包运行与发布协作**：能产出带运行壳的可玩包；A5 的“工程 zip 备份”不等于独立游戏包。
   Q1/Q2 完成后具备 `capture` profile、Content Studio 窄录制适配与分镜映射。第二阶段只验证技术闭包，
   不以替换全部版权素材或公开发行作为退出条件。

### 10.3 执行波次

以下 `R0-R9` 是执行波次，不是新的 schema id，也不替代能力地图格号。表中的 A7/A8、Q1/Q2 是预留工作流名称；正式开卡时再按三贤人流程写入能力地图，不能仅凭本路线图提前标成已实现能力。

| 波次 | 主任务 | 关键产物 | 退出条件 |
|---|---|---|---|
| R0 | 基线收口 | 路线图、能力地图、任务看板口径统一 | 本节落库；当前工作只从看板进入 |
| R1 | **N6 共享脚本/子程序** | 作者脚本库、引用编辑、保存/重迁移闭环 | 同一脚本被多处调用，改一处全部生效；MG2 重迁不丢；三方验收 |
| R2 | 迁移残余收口 | 单一 clean 脚本模型、全地址索引、阻塞式迁移诊断、场景脚本三态覆写 | PAL 产物零可执行 `unmigrated`，运行时零第二解释器；未知可达命令写盘前失败；双跑零计划与 M3 体积门禁继续成立 |
| R3 | **A7 工程资源闭包** | 资产引用审计器、统一 `FileSource + manifest` 加载链、资源闭包报告 | 工程内容只经 catalog/FileSource，engine chrome 只经 bundler registry；断开仓库外部素材目录后两条链仍分别完整 |
| R4 | **Q1 剧情 E2E 地基 + 前 10 段** | 分段格式、runner、检查点存档链、流程战斗速胜模式、前 10 段脚本；同时建立四种 N6b 取物 canary 与一条编辑器保存重开试玩基线 | 001 从新游戏起；002-010 均可由前段真实结束档独立启动并通过；取物与编辑器基线按业务结果断言且不依赖 raw AST；失败能定位到具体段 |
| R5 | **Q1 全流程扩展到数百段** | 全剧情碎片脚本、连续检查点、覆盖报告；最终段覆盖 s281 最终战→结局演出→`quitToTitle` 回标题 | 从开场到结局无断链；任意一段可单跑；相邻段存档来源可追溯；最终段到 `?menu` |
| R6 | 剩余引擎/编辑器能力收口 | 半完成格与创作工作流缺口（X3 结局流已由 R2 完成，不再列为产品缺口） | 能力地图中第二阶段目标格按用户裁决收口，编辑器与引擎两侧同时满足 done |
| R7 | 资源注册表整理 | 音乐等资源改成“稳定语义 id -> 明确资产路径/元数据”，清理隐式文件名索引 | `music.json` 等不再靠读者猜 id 与文件名关系；迁移兼容、编辑器引用与运行时解析全链通过 |
| R8 | ~~自有资源替换~~（移交第三阶段） | 第二阶段只保留资源导入、替换、来源记录与自包含能力 | 不计入第二阶段退出条件；第三阶段重新开卡排期 |
| R9 | **A1 + A8 第二阶段发布收口** | 服务器版本化预制工程资源库；运行壳 + 工程内容/资产 + 构建/校验命令 | 主体能力与 Q1/Q2 已闭合；新工程可下载确定版本的预制包；独立包在不依赖本仓库源码、`data/`、`projects/` 外部目录的干净环境中可启动并完成代表性 E2E |

R9 内部顺序为 A1 预制发布源在前、A8 独立包验收在后。A1 只在第二阶段最终发布前实施，不进入当前
短任务候选；R8 版权素材替换仍按用户裁决移交第三阶段，不是 A1 的隐含完成条件。

SceneIndex 先以 content20 原子切换；R4 薄基线直接建立在 content20。R4 通过后、R5 数百段扩展前插入
**N6b-v21 原子切换**：只实现第二阶段窄版，重生成 canonical
工程和版本绑定 checkpoint，再用同一 R4 业务断言回归。完整 Q1、Q2、编辑器综合 E2E、`capture` 和
A1/A8 一律建立在 content21 上，避免先铺满旧版资产再整体重做。

E6-1、四条 PAL 数字角色条件引用与 ED-3 统一引用边均已闭合；当前以 ED-3 为地基依次完成
场景/商店生命周期。完成这些确定性欠账后才进入 R4 薄 E2E 基线。上述 N6b 插入顺序只描述 R4 之后的版本切换
位置，不得据此越过前置欠账。

**2026-09-03 当前第二阶段完整队列（用户以历史清单复核）**：

1. [场景生命周期](../ops/tasks/ED-SCENE-LIFECYCLE-1-scene-crud-and-safe-delete.md)：复制、可读命名、
   引用反查、安全删除、撤销、保存重开与试玩。
2. [商店生命周期](../ops/tasks/ED-SHOP-LIFECYCLE-1-shop-crud-and-safe-delete.md)：复制、沿用货单派生目录标题、
   buy-only 引用保护、安全删除、保存重开与正式试买；不新增 ShopDef 名称或 schema 版本。
3. R4 薄 E2E：Q1 runner + 001-010、四种取物 canary、一条编辑器保存重开试玩链。
4. N6b 窄版 content21 原子切换，并复跑同一薄基线。
5. Q2 完整战斗专项五批（吸收 B5 和已知战斗操作/动画/音效缺口），随后扩 Q1 全通关链。
6. 敌队大目录虚拟滚动；必须在编辑器综合工作流 E2E 前完成。
7. 编辑器综合 E2E：空白工程创建完整 RPG 内容、保存、重开并试玩。
8. Q1/Q2 验证完成后建设 `capture` profile、Content Studio 窄录制适配与分镜映射。
9. A1 服务器版本化预制工程资源库。
10. A8 独立可玩包；以干净环境代表性 E2E 作为第二阶段最终退出门。

E6-1 已于 2026-09-04 三方 accept + 用户验收 done，故从当前队列移出；历史证据保留在任务卡。

Q2 当前已知问题至少包括：敌方全体仙术仍统一落在屏幕中心；杂项菜单“围攻/状态”灰显且无行为；
作者配置非伤害合体技时只扣代价并记日志、不执行效果；角色濒死/阵亡声及敌方部分攻击音效只有数据、
缺完整播放事件。它们进入 Q2 对应批次修复与回归，不用已有 PAL 数据暂未触发来宣称无缺口；Q2 仍须
继续发现并登记其他规则、操作、AI、状态、动画、音画和结算问题。

R1 的 N6 已完成。R6 的 ED-2 与 W7F 也已完成，其中 W7E 双格式方案被取消，W7F 已把迁移图和作者图统一为
ProjectMapV2。[R2 事件脚本单一模型与 unmigrated 退役](../ops/tasks/R2-script-single-model.md) 已于 2026-07-14 完成
三方复验与用户验收；其 s281 代表 E2E 已证明结局流实现，后续全流程持续回归归 Q1；
任何 E2E 暴露的迁移器根因仍遵守“先修上游、再重生成”的最高优先级规则。

### 10.3.1 当前复核与推荐顺序（2026-07-14）

本轮对“旧格式/新格式并行、只读迁移资产、编辑功能未闭环”做了同类复核。按依赖关系推进：

| 顺序 | 工作 | 已确认的问题 | 完成后进入 |
|---:|---|---|---|
| 1 | **R2 脚本单一模型** | **done（2026-07-14，三方复验 + 用户验收）**：原 66 个残余全部归类收口；17 个假缺失改按数组地址解析；产物零可执行 `unmigrated`，旧 opcode 第二解释器已删除 | R3/R7 |
| 2 | **R3/A7 + R7 资源闭包/注册表** | **done**：current-only 实现与 ARCH-CURRENT-ONLY-1 三方/用户验收均已完成；当前 schema 为 content20 / SAVE8。PAL 1,934 catalog records，56 个 effect sprite 已物化；editor/reforge 无 extracted/legacy fallback，旧版本发布链已删除 | ED-3；同时具备启动 Q1 的资源前提 |
| 3 | **ED-3 工程引用图** | **done（2026-09-05，三方 accept + 用户验收）**：同步领域、媒体 locator 与异步 map/tileset/stamp proof 已统一；临时 map helper、旧 DTO/scanner/string parser 已退役 | 场景/商店生命周期 |
| 4 | **创作生命周期** | 角色、物品、技能、毒等既有生命周期已闭合；场景仍缺复制/可读命名/安全删除，商店仍缺复制/安全删除；两者均缺保存重开和独立试运行 | ED-SCENE-LIFECYCLE-1 / ED-SHOP-LIFECYCLE-1 |
| 5 | **W8 地图内容选择与属性编辑 → W7G 组合地物图章 + 效率层** | **done（2026-07-18 后续批次）**：单/多格选择、实例属性、持久组合模板/placement、整体变换、生命周期与统一排序控件均已闭合；当前 ProjectMap v4 继续使用同一运行格式 | ED-3 与当前生命周期欠账 |

Q1/Q2 与独立打包目标没有取消：ED-3 和场景/商店生命周期这些确定性欠账完成后启动 R4/Q1 runner；
以上任一阶段发现迁移上游缺陷时，R2 类修复立即插队。W8/W7G 已完成且共同使用当前 ProjectMap v4；
下列小节保留其设计约束与历史证据，不再表示待实施状态。

#### W8 地图内容选择与实例属性编辑（2026-07-17 新登记）

- “瓦片素材选择”“待盖图章模板”“地图中已放置内容的选区”“当前活动图层”是四种独立状态，不能再共用一个
  `selectedTile` 或 `selection` 概念。取样只形成下一笔画刷，不能冒充对已有内容的编辑。
- 专用选择工具支持单格、拖拽框选、增选/减选、清空；右侧 Inspector 把视觉实例
  (`layerId + row + col` 的 tile 与实例遮挡高度)和格点属性(`row + col` 的独立碰撞)分区显示。多选不同值显示“混合”，
  修改某一字段不得改写其他字段。
- 默认只命中当前活动图层；隐藏层不命中也不可写，锁定层可见但不命中也不可写。其他层变暗只改变显示，不能偷偷改变
  编辑目标；重叠位置通过显式候选列表或 `Alt/Option + 点击`切换，图层改名/重排后仍以稳定 `layerId` 保持选择。
- 移动、复制、剪切、粘贴、删除和批量属性修改都必须是一次可逆命令。普通选区是否包含独立碰撞必须显式显示，不能因
  碰撞叠加层是否可见而暗中改变操作范围。
- 选区是编辑器工作区临时态，不写入游戏内容；属性修改才进入 `EditSession` 并保存。完整设计、测试矩阵和三方门禁见
  [W8 任务卡](../ops/tasks/W8-map-content-selection-inspector.md)。

#### W7G 组合地物 / 图章的已拍板需求

完整 schema 设计、S1-S16 未决题、交互/权限矩阵、测试清单和三方门禁见
[W7G 任务卡](../ops/tasks/W7G-composite-tile-stamps.md)。该卡已于 2026-07-18 完成三方验收与用户收口；
下列内容是已落地设计约束，不是当前 draft。

- 树、桌椅、屋顶、岩壁等由多个 tile 组成的地物，在编辑器里作为一个可预览、可命名、可分类的图章选择；
  用户画一棵树只需一次落笔，画 100 棵不再重复找碎片。
- 图章模板保存每个组成格的相对位置、视觉图层、tile、**实例高度**与碰撞值；这些属性在制作模板时设置一次，
  每次盖章自动带入，绝不能要求放置后逐 tile 补高度。
- 支持从地图框选“保存为图章”和由 tileset 预置图章；落笔前显示完整幽灵预览，越界/冲突有明确反馈。
- 一次盖章是一个原子命令，可整体撤销/重做；底层仍写入同一 ProjectMap 版本族的普通层、实例高度和独立碰撞，
  运行时不认识图章，也不形成 StampMap/Tilemap 之类第二格式。
- 用户必须能在盖章后、切换工具后以及保存重开后，通过点击任一组成格重新选中整个已放置图章。仅把一次盖章摊平成
  普通格无法可靠区分两个相邻同款图章，因此 W7G 正式设计必须加入**稳定、非链接的作者态放置组身份**；其 schema
  形状、覆盖/重叠/残缺成员/图层删除规则必须在 W7G 自己的三方设计卡中定案，不能偷塞进 W8。
- “整章选择”和“进入组内改单格”必须是明确的两级操作，并提供“解组但保留格子”。图章模板修改或删除不联动既有放置；
  这不是 linked prefab。跨图层图章的移动、复制、删除和撤销必须全有或全无，任一目标层隐藏/锁定/缺失时不得静默只改一部分。

### 10.4 Q1 全流程 E2E 硬契约

[碎片地图与检查点台账](../../projects/pal/e2e-checkpoints/README.md)继续作为每段边界与验证点的真值。实现不得把它降格成几条冒烟测试：

- 整个游戏切成数百个小段；每段有可执行流程脚本、明确起点/终点和断言。
- 001 从全新游戏开始。除 001 外，每段起始存档必须由上一段**真实执行后的结束状态**生成；禁止手工伪造一份看似能跑的剧情状态替代链路。
- 第 N 段的结束存档就是第 N+1 段的起始存档；文件记录来源段、生成提交和必要的内容版本，能检查链路漂移。
- Q1 只验证剧情、场景、演出、菜单接续和存档连续性。遇到任何战斗，runner 直接取得胜利结果，但仍走正式胜利结算、奖励和胜利剧情分支，保证下一段存档合法。
- 战斗系统另建 **Q2 隔离测试**：固定队伍/敌队/随机种子，覆盖指令、公式、AI、状态、动画与结算；Q2 禁止使用 Q1 的速胜旁路。
- 前 10 段先证明 runner、检查点和调试成本可控，再批量扩展。R5 是长期流水线，可与 R6-R7 穿插，但存档链必须按顺序增长，不能跳段造档。

**2026-09-03 用户确认 Q1 / Q2 边界**：完整通关 Q1 会跳过真实战斗或让战斗直接胜利，因此 Q2
必须作为独立的完整战斗专项 E2E，补齐 Q1 有意留下的测试盲区。Q2 正式开工时使用一张父任务卡，按
测试入口与覆盖台账、规则与操作、AI/状态/特殊机制、动画与音画表现、完整战斗生命周期五批连续推进；
B5 战斗表现并入第四批，不再用一次只读表现盘点冒充完整战斗验收。当前只记录已确认边界，尚未开卡承诺。

**2026-09-05 版本顺序追加裁决**（取代 2026-09-03 的具体版本号，不改变“薄基线先于 N6b”）：
SceneIndex 先原子切换 content20；R4 直接在 content20 上交付可重复的薄基线；随后 N6b 切 content21、
重生成当前工程并复跑同一业务合同，再进入 R5 与完整 Q2。E2E 以剧情/场景/库存/实体生命周期/战斗结果等
语义为断言，不得绑定命令条数或 raw JSON；版本 checkpoint 失效时按 current-only 规则重建，不加 upgrader。

**第二阶段发布录制链（2026-09-03 用户确认）**：Q1/Q2 的 `verify`、checkpoint 链和执行回执全部成立后，
再建设 `capture` profile、Content Studio 窄录制适配与分镜映射。三者服务第二阶段发布，仍属第二阶段；
但它们只能消费已验证碎片，不能在 E2E 前抢跑，也不能用“录得出来”替代剧情、战斗和状态断言。
敌队大目录虚拟滚动同样留在第二阶段，作为编辑器综合 E2E 前的性能增强。

### 10.5 工程自包含与发行边界

- **A5 工程 zip**：用于备份/交换可编辑工程，内容是工程目录本身。
- **Engine chrome**：默认 UI、字形、光标与默认标题属于运行壳，由 Reforge/editor play 的 bundler 产物携带，不进入工程 catalog 或 A5 ZIP。
- **A7 资源闭包**：保证这个工程目录确实包含运行所需的全部项目资源，所有加载都经 manifest 与 `FileSource`。
- **A8 独立发行包**：在 A7 之上再带可执行的 Web 运行壳和版本化构建产物，面向玩家，不要求玩家安装仓库依赖或打开编辑器。

A7 必须先于大规模 E2E：否则流程跑到后半段才发现某类头像、音乐、RNG、字体或音效仍从仓库外取，会让检查点和发行验证一起返工。

### 10.6 版权资源替换边界（移交第三阶段）

- 第二阶段只交付资源清单、引用闭包、导入/替换工作流、来源元数据和自包含打包能力，不要求把
  `projects/pal` 的原版受保护素材实际批量替换完。
- 批量生成或授权素材、逐族替换、视觉回归以及公开发布前的版权门禁统一归
  [第三阶段版权资源替换](../phase3/future-gameplay-and-mmo-backlog.md#0-发布与版权资源替换)。
- 第二阶段不得为了提前清版权而改写已经核实的内容、动画或资源语义；第三阶段替换时仍复用 A7
  已完成的稳定 AssetId、引用图、作者资源和分发链。
